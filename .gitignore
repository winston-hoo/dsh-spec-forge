# 依赖：本目录是 Windows junction，指向本机 ~/.dsh/profiles/node_modules/@deepseek-ai。
# 绝不能提交——会把指向 C 盘的链接当内容入库。clone 到新机器后按 README「本机开发」重建。
node_modules/

# 本地开发专属文件：含本机绝对路径，不发布
dev/
.local/

# 临时与残留
*.tmp
*.tsbuildinfo
.DELETE.*
coverage/
test-out.txt
test-err.txt

# 插件自己的运行时数据目录（workspace 模式下就在仓库根）：模板库、档案、诊断埋点。
# 跑测试时 boot() 会按默认模式写到这里——绝不入库。
.dsh-spec-forge/
prestep-trace.log
